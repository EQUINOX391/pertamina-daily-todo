function RegisterPage() {
  return (
    <section className="page-card">
      <h2>Register</h2>
      <p>
        Register form will be implemented after the routing and API client
        foundation are ready.
      </p>

      <div className="placeholder-box">
        <p>Planned endpoint:</p>
        <code>POST /api/auth/register</code>
      </div>
    </section>
  );
}

export default RegisterPage;
